[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "NODE_ENV=production node backend/dist/server.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[deploy.healthcheckPath]]
path = "/health"
